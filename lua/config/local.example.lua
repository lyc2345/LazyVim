-- Copy this file to `lua/config/local.lua` for machine-specific settings.
-- Keep `local.lua` out of git if it contains private or machine-only values.

return {
  -- Set to true on machines where GitHub/plugin update checks are blocked.
  nvim_updates_disabled = false,
}
